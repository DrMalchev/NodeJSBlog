const numbersArray = [1, 2, 3, 4, 5]

let mappedArray = numbersArray.map(num => `Number = ${num}`)

const names = ["John", "Carry", "David", "Maria"]

module.exports = {
    numbers: mappedArray,
    names: names
}